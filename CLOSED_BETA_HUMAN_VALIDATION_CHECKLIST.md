# Closed Beta 人工真机验收清单

本清单用于后续真实手机/浏览器验收。每项记录：设备、浏览器、时间（含时区）、操作结果、异常截图/页面路径、HTTP/API 错误时间、是否可复现、刷新/重试结果。不要使用真实生产破坏性操作测试；Restore 只使用准备好的测试备份，并先确认 BeforeRestore。弱网/离线时记录是否出现假成功、无限加载或重复提交。

1. **注册 / 登录**
   - 操作：注册测试账号，完成验证后登录、退出、重新登录。
   - 预期：页面可进入，刷新后会话稳定；错误密码有清晰提示。
   - 记录：验证邮件、登录跳转、退出后是否仍能访问业务页。

2. **房源**
   - 操作：创建、编辑、归档一套测试房源。
   - 预期：名称、地址、状态保存正确，返回列表后仍一致。
   - 记录：保存结果、刷新结果、窄屏布局。

3. **房间**
   - 操作：在测试房源下创建、编辑、归档房间。
   - 预期：房源关系正确，房间状态可理解。
   - 记录：房间数量、状态变化、按钮是否被遮挡。

4. **入住**
   - 操作：为测试房间创建租客并完成入住。
   - 预期：已占用房间不能重复入住，并显示可理解的冲突提示。
   - 记录：租客、房间、合同和押金状态。

5. **收租**
   - 操作：记录一笔正常收租。
   - 预期：金额、日期、支付状态和租客月度状态一致。
   - 记录：保存结果、刷新后金额、重复点击行为。

6. **欠租 / 部分收租**
   - 操作：记录部分收款并查看欠租状态。
   - 预期：已收、未收和提醒状态不重复计算。
   - 记录：金额、月份、欠租提示和统计变化。

7. **押金**
   - 操作：新增押金记录并执行允许的状态操作。
   - 预期：押金金额、类型、日期和租客关系正确。
   - 记录：状态反馈和重复提交行为。

8. **支出**
   - 操作：新增一笔测试支出。
   - 预期：金额、分类、日期和房源关系正确。
   - 记录：保存、刷新和统计结果。

9. **换房**
   - 操作：把测试租客换到空房，再尝试换到已占用房间。
   - 预期：成功换房后关系一致；占用冲突返回明确 409 语义且不产生部分变更。
   - 记录：原房间、目标房间、租客/合同状态。

10. **退租**
    - 操作：对测试租客执行退租/归档流程。
    - 预期：历史记录保留，房间状态正确，未出现永久删除入口。
    - 记录：退租日期、历史列表、房间状态。

11. **Reminder**
    - 操作：查看并处理一条测试提醒。
    - 预期：提醒对象、月份、状态和跳转目标正确。
    - 记录：提醒入口、返回路径、已处理状态。

12. **Analytics**
    - 操作：查看统计、利润、空置和已出租状态。
    - 预期：金额不出现 NaN/Infinity，空置橙色与已出租绿色清晰，手机可横向查看图表。
    - 记录：数据范围、图例、图表滚动和主题模式。

13. **Backup**
    - 操作：创建官方 JSON 备份；另行查看 Excel/CSV 导出。
    - 预期：JSON 是可用于 Restore 的完整业务备份；Excel/CSV 仅为报表，页面不暗示自动云备份已开启。
    - 记录：文件名、下载/保存结果、格式说明。

14. **Restore**
    - 操作：选择测试 JSON，先看预览/dry-run，再确认 BeforeRestore 后恢复。
    - 预期：预览不改数据；确认步骤明确；失败有回滚提示。
    - 记录：预览差异、BeforeRestore 文件、最终数据等价性。

15. **Logout / relogin**
    - 操作：退出账号，再登录另一测试账号并刷新。
    - 预期：旧账号数据不串入新账号；回退路径和缓存已清理。
    - 记录：地址栏、列表、账户信息和浏览器存储异常。

16. **手机返回 / 刷新**
    - 操作：在 390x844、393x852 或 430x932 视口中返回、刷新、关闭弹窗。
    - 预期：底部导航不遮挡内容，弹窗可关闭，输入框和按钮可操作。
    - 记录：横向溢出、裁切、键盘遮挡和安全区问题。

17. **长期 Session**
    - 操作：保持页面一段时间后刷新并继续读取业务数据。
    - 预期：有效会话继续工作；过期会话转到登录，不显示错误数据。
    - 记录：刷新时间、会话状态、是否需要重新登录。

18. **PWA 重新打开**
    - 操作：从主屏幕/独立窗口重新打开应用，再执行返回、刷新和退出登录。
    - 预期：加载最新页面，不被旧缓存卡住；退出后不能看到旧账号数据。
    - 记录：设备、PWA/浏览器模式、版本、缓存异常和恢复方式。

SMTP、Production Synthetic Auth session 和 Production Synthetic Drill 的阻塞项不在本清单内，待对应前置条件满足后单独执行。
