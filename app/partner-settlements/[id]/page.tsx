"use client";

import { AppLayout } from "@/components/app-layout";
import { useAccountAccess } from "@/components/account-access";
import { euro } from "@/lib/format";
import { getValidSupabaseSession } from "@/lib/supabase";
import { useEffect, useState } from "react";

type SnapshotData = { batch: any; partners: any[]; segments: any[]; transfers: any[] };

export default function PartnerSettlementSnapshotPage({ params }: { params: Promise<{ id: string }> }) {
  const access = useAccountAccess();
  const [data, setData] = useState<SnapshotData | null>(null);
  const [message, setMessage] = useState("");
  const [reason, setReason] = useState("");

  useEffect(() => { void params.then(async ({ id }) => { const session = await getValidSupabaseSession(); if (!session) { setMessage("登录已失效"); return; } try { const response = await fetch(`/api/partner-settlements/${id}`, { headers: { Authorization: `Bearer ${session.access_token}` }, cache: "no-store" }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error || "加载结算快照失败"); setData(payload); } catch (error) { setMessage(error instanceof Error ? error.message : "加载结算快照失败"); } }); }, [params]);

  async function reverse() {
    if (!data || !reason.trim() || !window.confirm(`确认撤销该结算快照吗？\n\n房源：${data.batch.property_name_snapshot || data.batch.property_id}\n期间：${data.batch.period_start} 至 ${data.batch.period_end}\n净利润：${euro(Number(data.batch.net_profit))}\n确认时间：${new Date(data.batch.confirmed_at).toLocaleString("zh-CN")}`)) return;
    const session = await getValidSupabaseSession(); if (!session) { setMessage("登录已失效"); return; }
    const response = await fetch(`/api/partner-settlements/${data.batch.id}`, { method: "POST", headers: { "Content-Type": "application/json", Authorization: `Bearer ${session.access_token}` }, body: JSON.stringify({ reason: reason.trim() }) });
    const payload = await response.json().catch(() => ({}));
    if (!response.ok) { setMessage(payload.error || "撤销失败"); return; }
    setMessage("结算已撤销"); setData({ ...data, batch: { ...data.batch, status: "reversed", reversal_reason: reason.trim(), reversed_at: new Date().toISOString() } });
  }

  if (message === "登录已失效" && !data) return <AppLayout title="登录已失效"><section className="card panel auth-expired-state"><h2 className="panel-title">登录已失效</h2><p className="muted">请重新登录后继续查看结算快照。</p><a className="btn primary" href={`/login?returnTo=${encodeURIComponent("/partner-settlements")}`}>重新登录</a></section></AppLayout>;
  if (message && !data) return <AppLayout title="结算快照"><section className="card panel"><p className="error-text">{message}</p></section></AppLayout>;
  if (!data) return <AppLayout title="结算快照"><section className="card panel"><p className="muted">加载中…</p></section></AppLayout>;

  const batch = data.batch;
  const incomeDetails = Array.isArray(batch.income_details_snapshot) ? batch.income_details_snapshot : [];
  const expenseDetails = Array.isArray(batch.expense_details_snapshot) ? batch.expense_details_snapshot : [];
  return <AppLayout title="结算快照" description="历史快照只读取确认时保存的数据。"><section className="card panel settlement-snapshot-page"><div className="panel-header"><div><h2 className="panel-title">{batch.property_name_snapshot || batch.property_id}</h2><p className="muted">{batch.period_start} 至 {batch.period_end} · <span className={`status-badge ${batch.status === "confirmed" ? "success" : "muted-badge"}`}>{batch.status === "confirmed" ? "已结算" : "已撤销"}</span></p></div><a className="btn compact" href="/partner-settlements">返回历史</a></div><div className="snapshot-meta"><span>确认时间：{new Date(batch.confirmed_at).toLocaleString("zh-CN")}</span><span>确认账号：{batch.confirmed_by_display_name_snapshot || batch.confirmed_by_account_id || "未保存"}</span>{batch.reversed_at ? <span>撤销时间：{new Date(batch.reversed_at).toLocaleString("zh-CN")}</span> : null}{batch.reversed_by_account_id ? <span>撤销账号：{batch.reversed_by_account_id}</span> : null}{batch.reversal_reason ? <span>撤销原因：{batch.reversal_reason}</span> : null}{batch.note ? <span>备注：{batch.note}</span> : null}</div><div className="compact-report-card"><div>总收入 <strong>{euro(Number(batch.total_income))}</strong></div><div>总支出 <strong>{euro(Number(batch.total_expense))}</strong></div><div>净利润 <strong className={Number(batch.net_profit) < 0 ? "danger-text" : "profit"}>{euro(Number(batch.net_profit))}</strong></div></div><details className="snapshot-section" open><summary>比例分段（{data.segments.length}段）</summary>{data.segments.map((segment) => <div className="snapshot-segment" key={segment.id}><strong>{segment.segment_start} 至 {segment.segment_end}</strong><span>收入 {euro(Number(segment.total_income))} · 支出 {euro(Number(segment.total_expense))} · 净利润 {euro(Number(segment.net_profit))}</span><pre>{JSON.stringify(segment.shares_snapshot, null, 2)}</pre></div>)}</details><details className="snapshot-section" open><summary>合伙人结算明细（{data.partners.length}人）</summary><div className="settlement-grid">{data.partners.map((partner) => <article className="settlement-card" key={partner.id}><div className="profit-card-head"><div><strong>{partner.partner_display_name_snapshot}</strong>{partner.legacy_code_snapshot ? <small className="muted">兼容归属代码 {partner.legacy_code_snapshot}</small> : null}</div><span className="status-badge">结算快照</span></div><div className="profit-card-metrics"><span>代收 <b>{euro(Number(partner.actual_collected))}</b></span><span>垫付 <b>{euro(Number(partner.actual_paid))}</b></span><span>实际留存 <b>{euro(Number(partner.actual_retained))}</b></span><span>应得利润 <b>{euro(Number(partner.profit_entitlement))}</b></span><span>结算余额 <b>{euro(Number(partner.settlement_balance))}</b></span></div></article>)}</div></details><details className="snapshot-section" open><summary>转账建议（{data.transfers.length}笔）</summary>{data.transfers.length ? data.transfers.map((transfer) => <p key={transfer.id}>{transfer.from_name_snapshot} → {transfer.to_name_snapshot}：{euro(Number(transfer.amount))} {transfer.currency || "EUR"}</p>) : <p className="muted">无需转账</p>}</details><details className="snapshot-section"><summary>收入归属明细（{incomeDetails.length}笔）</summary>{incomeDetails.length ? incomeDetails.map((item: any, index: number) => <p key={item.paymentId || index}>{item.date} · {item.partnerName} · {item.incomeItem || "收入"} · {euro(Number(item.amount))}</p>) : <p className="muted">该快照确认时未保存逐笔收入明细，不能用当前账目补造。</p>}</details><details className="snapshot-section"><summary>支出归属明细（{expenseDetails.length}笔）</summary>{expenseDetails.length ? expenseDetails.map((item: any, index: number) => <p key={item.expenseId || index}>{item.date} · {item.partnerName} · {item.category || "支出"} · {euro(Number(item.amount))}</p>) : <p className="muted">该快照确认时未保存逐笔支出明细，不能用当前账目补造。</p>}</details>{batch.status === "confirmed" && access.isOwner ? <div className="snapshot-reversal"><div className="field"><label>撤销原因（必填）</label><textarea value={reason} onChange={(event) => setReason(event.target.value)} placeholder="请填写撤销原因" /></div><button className="btn danger" type="button" disabled={!reason.trim()} onClick={() => void reverse()}>撤销结算</button></div> : null}{message ? <p className="success-text">{message}</p> : null}</section></AppLayout>;
}
